#include<stdio.h>
int main()
{
	int base, exponent, power, i;
	printf("Enter x value:");
	scanf("%d", &base);
	printf("Enter y value:");
	scanf("%d", &exponent);
	
	power=1;
	i=1;
	
	while(i<=exponent)
	{
		power = power * base;
		i++;
	}
	printf("Answer:%d", power);
	return 0;
}
